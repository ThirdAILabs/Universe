name: Run Python Tests

# Kills old jobs from the same pr if we push a new commit
# See https://stackoverflow.com/questions/66335225/how-to-cancel-previous-runs-in-the-pr-when-you-push-new-commitsupdate-the-curre
concurrency: 
  group: pytest-${{ github.head_ref }}
  cancel-in-progress: true
  
# Controls when the workflow will run
on:
  # Run on push, pull, and manually.
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  python-unit-tests:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, which is the CWD for
      # the rest of the steps
      - uses: actions/checkout@v2
        with:           
          submodules: 'recursive'
          token: ${{ secrets.CICD_REPO_ACCESS_TOKEN }}
        
      - name: Build python package
        run: bin/build.py

      - name: Install necessary python pacakges
        run: pip3 install numpy pytest toml mmh3 datasets torch transformers

      - name: Run python unit tests
        run: bin/python-test.sh -m unit