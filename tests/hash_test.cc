#include <gtest/gtest.h>
#include "tabulationhash/UniversalHash.h"
#include "murmurhash/MurmurHash3.h"

TEST(testHashes, MurmurHashSpeedTest) {
    // TODO(alan): Add test cases.
    EXPECT_EQ(10, 10);
}

TEST(testHashes, TabulationHashSpeedTest) {
    // TODO(alan): Add test cases.
    EXPECT_EQ(10, 10);
}