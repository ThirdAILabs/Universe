# - test that the dataset produces the right number of batches
# - test that shuffle works
# - test that no shuffle -> not load whole dataset
# - make custom block that produces random values, but save these random values. Make sure output matches. Test for both sparse and dense.